interface:
  display_name: "NERO Design Team"
  short_description: "Routes NERO design work through brand, tokens, generators, cases, QA, scoring, and production checks."
  brand_color: "#1F4E5F"
  default_prompt: "Use $nero-design-team to create or audit a professional NERO artifact. Classify the route, load brand guidance and case references, build/load tokens, use the generator or template, brief gpt-image-2 only for visual素材 when needed, then run visual QA, scoring, and production check."

policy:
  allow_implicit_invocation: true
