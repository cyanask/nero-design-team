interface:
  display_name: "NERO Design Team"
  short_description: "Task-led design, open references, methods and visual review."
  brand_color: "#1F4E5F"
  default_prompt: "Use $nero-design-team to understand the goal, inspect local and web references as needed, compare directions, compose a trial and improve it from actual renders while preserving the task constraints."

policy:
  allow_implicit_invocation: true
